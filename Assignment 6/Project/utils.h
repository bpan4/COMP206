//#include "utils.c"
void parse(char *s, int *co, int *ex);
//accepts a string and two integer pointers as its argument
//parses a line retrieved from the fgets function in main and stores the values in pointers co and ex
int powi(int x, int ex);
//computes x to the power of ex and returns the value obtained
